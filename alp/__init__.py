"""ALP (Adaptive Lossless floating-Point) compression — Python port.

Public API (planned for 0.8.0):

    from alp import encode, decode
"""

__version__ = "0.8.0"

# Encoder/decoder functions land in 0.8.0. Skeleton release just imports the
# version constant so `python -c "import alp; print(alp.__version__)"` works.
